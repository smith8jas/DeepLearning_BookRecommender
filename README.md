preprocessing.py
→ clean_text()
→ combine title + authors + categories + description

recommender.py
→ create TF-IDF / BoW vectors
→ calculate cosine similarity
→ recommend_books()

app.py
→ Gradio interface

utils.py
→ load data, save data, helper functions

